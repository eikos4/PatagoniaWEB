# Copia a .env para desarrollo local (no subir .env a Git)
SECRET_KEY=cambia-esta-clave-en-produccion
FLASK_DEBUG=true
FLASK_ENV=development

# Opcional: PostgreSQL en producción (Render lo inyecta automáticamente)
# DATABASE_URL=postgresql://user:pass@host/db

# Admin inicial (ejecutar: python ventas.py)
ADMIN_EMAIL=carlos@patagoniasur.cl
ADMIN_PASSWORD=tu-password-seguro

WHATSAPP_PHONE=56920576206
CONTACT_EMAIL=contacto@patagoniasur.cl
